spring:
  config:
    import:
    - config.yml
    - datasource.yml

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        #format_sql: true

logging:
  level:
    org:
      springframework.web.servlet.mvc.method.annotation.ExceptionHandlerExceptionResolver: off
